'use strict';

module.exports = function(Rating) {

/*
    GenericModel.find({ where: { UUID : id } },  function(err, ratings) {
        if(err) {
            console.log(err);
        } else {
            post.rating = ratings;
            cb(null, post);
        }
    });
*/
};
